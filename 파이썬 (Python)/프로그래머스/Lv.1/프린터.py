from queue import Empty


def solution(priorities, location):

    for i in priorities:
        for
        
    
    


    # length - 1 의 값이면 + 1 이 아닌  = 0으로 초기화 해주기
    if i == len(priorities)-1 :
        i =0

print(solution([2, 1, 3, 2],2))